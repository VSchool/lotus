import React from "react"
import { Button } from "@vschool/lotus"
import styled from "styled-components"

export default () => (
    <Button color="gray" uppercase inverse>
        Hello world!
    </Button>
)
